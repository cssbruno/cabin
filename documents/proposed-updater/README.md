# Archived updater draft

The production implementation is now in `app/src/main/kotlin/com/cabin/updates/`. Cabin alpha.3 enables daily metadata checks, manual APK downloads, and Android-approved installation from `cssbruno/cabin`. The old Kotlin files in this directory are historical drafts and are not compiled.
